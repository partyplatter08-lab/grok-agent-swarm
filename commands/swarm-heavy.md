---
description: Swarm Heavy — collaborative Heavy council plus K3-style Agent Swarm fan-out
argument-hint: "<task> [--council N] [--concurrency N] [--dry-run]"
---

# /swarm-heavy

Load and follow the **swarm-heavy** skill (`skills/swarm-heavy/SKILL.md` in this plugin).

User task and flags:

$ARGUMENTS

If empty, ask for a task that needs both deep multi-agent judgment and wide parallel execution.
